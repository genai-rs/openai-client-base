# BetaToolChoiceParam

## Enum Variants

| Name | Description |
|---- | -----|
| BetaSpecificApplyPatchParam | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaSpecificFunctionShellParam | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaSpecificProgrammaticToolCallingParam | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaToolChoiceAllowed | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaToolChoiceCustom | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaToolChoiceFunction | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaToolChoiceMcp | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaToolChoiceOptions | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |
| BetaToolChoiceTypes | How the model should select which tool (or tools) to use when generating a response. See the &#x60;tools&#x60; parameter to see how to specify which tools the model can call.  |

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


