# RealtimeSessionCreateRequestGaTracing

## Enum Variants

| Name | Description |
|---- | -----|
| String | Realtime API can write session traces to the [Traces Dashboard](/logs?api&#x3D;traces). Set to null to disable tracing. Once tracing is enabled for a session, the configuration cannot be modified.  &#x60;auto&#x60; will create a trace for the session with default values for the workflow name, group id, and metadata.  |
| TracingConfiguration1 | Realtime API can write session traces to the [Traces Dashboard](/logs?api&#x3D;traces). Set to null to disable tracing. Once tracing is enabled for a session, the configuration cannot be modified.  &#x60;auto&#x60; will create a trace for the session with default values for the workflow name, group id, and metadata.  |

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


