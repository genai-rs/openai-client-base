/*
 * OpenAI API
 *
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 *
 * The version of the OpenAPI document: 2.3.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ImagePartResource : Metadata for an image uploaded through ChatKit.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize, bon::Builder)]
pub struct ImagePartResource {
    /// Unique identifier for the uploaded image.
    #[serde(rename = "id")]
    pub id: String,
    /// Type discriminator that is always `image`.
    #[serde(rename = "type")]
    pub r#type: Type,
    /// MIME type of the uploaded image.
    #[serde(rename = "mime_type")]
    pub mime_type: String,
    /// Original filename for the uploaded image. Defaults to null when unnamed.
    #[serde(rename = "name", deserialize_with = "Option::deserialize")]
    pub name: Option<String>,
    /// Preview URL that can be rendered inline for the image.
    #[serde(rename = "preview_url")]
    pub preview_url: String,
    /// Signed URL for downloading the uploaded image. Defaults to null when no download link is available.
    #[serde(rename = "upload_url", deserialize_with = "Option::deserialize")]
    pub upload_url: Option<String>,
}

impl ImagePartResource {
    /// Metadata for an image uploaded through ChatKit.
    pub fn new(
        id: String,
        r#type: Type,
        mime_type: String,
        name: Option<String>,
        preview_url: String,
        upload_url: Option<String>,
    ) -> ImagePartResource {
        ImagePartResource {
            id,
            r#type,
            mime_type,
            name,
            preview_url,
            upload_url,
        }
    }
}
/// Type discriminator that is always `image`.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Type {
    #[serde(rename = "image")]
    Image,
}

impl Default for Type {
    fn default() -> Type {
        Self::Image
    }
}

impl std::fmt::Display for ImagePartResource {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match serde_json::to_string(self) {
            Ok(s) => write!(f, "{}", s),
            Err(_) => Err(std::fmt::Error),
        }
    }
}
